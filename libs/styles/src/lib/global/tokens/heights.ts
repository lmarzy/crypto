// ========================================================================
// TOKENS/HEIGHTS
// ========================================================================

import { heightsPx } from '../../settings';
import { pxToRem } from '../../utils';

export const heights = `
  --height-inputs: ${pxToRem(heightsPx.inputs)};
`;
